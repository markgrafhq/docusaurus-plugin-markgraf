# Example Docusaurus site

Minimal Docusaurus site that uses `@markgrafhq/markgraf-docusaurus`.

```bash
cd example
npm install
npm run start
```

Open http://localhost:3000/docs/markgraf — the page contains a ```` ```markgraf ```` fenced block that renders as a live player.
